const inputEmail = document.querySelector('#registration-input');
const inputPassword = document.querySelector('#registration-password');
const repeatInputPassword = document.querySelector('#repeat-registration-password');
const button = document.querySelector('form button');

window.addEventListener('load', () => {
    inputEmail.style.transform = 'scale(1)';
    inputPassword.style.transform = 'scale(1)';
    repeatInputPassword.style.transform = 'scale(1)';
    button.style.transform = 'scale(1)';
})